# Bootstrap/data inspection only.
# Install model-specific PyTorch/CUDA dependencies from the selected upstream
# repository after inspecting the actual machine GPU/driver.
datasets>=3.0,<5.0
huggingface_hub>=0.28,<2.0
Pillow>=10.0,<13.0
