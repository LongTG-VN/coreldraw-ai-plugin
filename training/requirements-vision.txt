# Optional local vision-critic stack; CI unit tests use fake critics and do not install this file.
# Match the CUDA wheel to the PyTorch runtime. The validated Windows stack is:
# torch==2.12.1+cu126 from https://download.pytorch.org/whl/cu126
torchvision==0.27.1
transformers==5.14.1
accelerate>=1.10,<2
bitsandbytes>=0.49,<0.50
