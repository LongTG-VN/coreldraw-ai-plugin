# Design AI v0.1 local QLoRA stack. Install PyTorch separately from the
# official CUDA wheel index documented in training/README.md.
transformers==5.14.1
peft==0.20.0
accelerate==1.14.0
bitsandbytes==0.50.0
safetensors>=0.8.0,<0.9
